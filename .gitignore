# BEGIN swamp managed section - DO NOT EDIT

# Runtime data (not needed in version control)
.swamp/

# Local extension sources (developer-specific, not shared)
.swamp-sources.yaml

# Claude Code configuration (managed by swamp)
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
# END swamp managed section

# Node/Deno local install (regenerated by npm ci)
node_modules/

# Local demo artifacts (personal B2 bucket / vault — not part of the published extension)
vaults/
models/@atalanta/restic-backup/

# Build-time model instances and the test-runner workflow (not part of the published extension)
models/
workflows/workflow-f1511907-67a5-44ca-9a39-95bbf9f19104.yaml

# Internal build proposal (references local KB paths; not for the public repo)
DESIGN.md
